package: nss

deps:
  - nss_copyright

slices:
  libs:
    deps:
      - nss_nss
      - nss_smime

  nss:
    deps:
      - glibc_libs
      - nspr_libs
      - sqlite_libs
      - nss-util_libs
    contents:
      common:
        /usr/lib64/libnss3.so:

  smime:
    deps:
      - glibc_libs
      - nspr_libs
    contents:
      common:
        /usr/lib64/libsmime3.so:

  copyright:
    contents:
      common:
        /usr/share/licenses/nss/COPYING:
