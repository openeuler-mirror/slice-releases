package: libselinux

deps:
  - libselinux_copyright

slices:
  libs:
    deps:
      - libc6_libs
      - libpcre2_libs
    contents:
      common:
        /usr/lib64/libselinux.so*:

  copyright:
    contents:
      common:
        /usr/share/licenses/libselinux/LICENSE: