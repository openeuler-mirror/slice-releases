package: expat

deps:
  - libexpat1_copyright

slices:
  libs:
    deps:
      - libc6_libs
    contents:
      common:
        /usr/lib64/libexpat.so.1*:
#        /usr/lib64/libexpatw.so.1*: (no)

  copyright:
    contents:
      common:
        /usr/share/licenses/expat/COPYING:
